# Decision log

Settled decisions, ADR-lite: one dated entry each, with the rationale in one breath and a pointer to the document that owns the full treatment. When an [open question](./open-questions.md) is resolved, it graduates to an entry here — that is the only way a question leaves that file. A cold reviewer should be able to distinguish "considered and rejected" from "never considered" using this log alone.

Entries are append-only. Reversing a decision gets a *new* entry that supersedes the old one, never an edit.

| ID | Date | Decision | Rationale (short) | Owner doc |
|----|------|----------|-------------------|-----------|
| D-001 | 2026-07-10 | **Build blizzard; do not adopt an existing orchestrator.** | Every surveyed orchestrator hard-codes a single-repo session model (session = repo = branch = PR); polyrepo is non-negotiable for winter workspaces. Steal ideas, not codebases. | [research/ecosystem.md](../research/ecosystem.md) |
| D-002 | 2026-07-10 | **Deterministic shell, intelligent core.** | All loop, arbitration, and recovery logic is deterministic code; LLMs supply judgment only at named points (work, planning, verdicts). | [product/vision.md](../product/vision.md) |
| D-003 | 2026-07-10 | **Broker on sqlite (WAL), not flock/Redis/git-native/GitHub-claims.** | Atomic CAS claims come free with ACID; no daemon to run; second-granularity heartbeats rule out git and GitHub as the fast path. | [design/runner/store.md](../design/runner/store.md) |
| D-004 | 2026-07-10 | **Facts-only schema; status is always derived, never a stored column.** | Written status lies after a crash; derived status tells the truth regardless of how the process died. Adopted from Agent Orchestrator. | [design/architecture.md](../design/architecture.md) |
| D-005 | 2026-07-10 | **GitHub owns *what work exists*; the local broker owns *who is working now*.** | Backlog is durable and shared; claims are fast and local. The supervisor's SYNC step reconciles the two. | [design/architecture.md](../design/architecture.md) |
| D-006 | 2026-07-10 | **Task identity is the GitHub issue, not a local id.** | Makes the solo→team transition a sync-policy change instead of a schema migration. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-007 | 2026-07-10 | **Fencing epochs on every claim, checked on the delivery path.** | Heartbeats alone cannot prevent a reaped-but-alive zombie from clobbering its successor; a stale epoch can be rejected deterministically. | [design/concurrency-model.md](../design/concurrency-model.md) |
| D-008 | 2026-07-10 | **Singleton-resource enforcement lives in a wrapper CLI at the resource boundary, not in harness hooks.** | Hook strength varies by harness (Codex weakest); a boundary the agent cannot route around works for all of them. Hooks are polish. | [design/harness-adapters.md](../design/harness-adapters.md) |
| D-009 | 2026-07-10 | **A missing verdict is a failure.** | The system fails safe: absence of a clear success signal is never read as success. | [design/runner/loop.md](../design/runner/loop.md) |
| D-010 | 2026-07-10 | **Ask-and-exit over a blocking `blizzard ask --wait`.** | Parking a task as a durable row is crash-equivalent to everything else; a process blocked on a human for hours is exactly the fragility the design avoids. A bounded-timeout compromise remains possible later. | [design/ask-answer.md](../design/ask-answer.md) |
| D-011 | 2026-07-10 | **Batching rollout ladder: strict 1:1 → deterministic heuristic → LLM planner in shadow mode → driving.** | Every rung is independently useful, and the 1:1 mapping remains the always-correct fallback a failed batch explodes back to. | [design/planner.md](../design/planner.md) |
| D-012 | 2026-07-10 | **Runners connect outbound-only; the hub never stores code or transcripts.** | The trust boundary is structural — the remote surface cannot leak what it does not hold, and nothing dials into a dev box. | [design/hub/index.md](../design/hub/index.md) |
| D-013 | 2026-07-11 | **Interoperability is the mission's core; winter + GitHub + Claude Code are the reference stack, bound through named seams (workspace, work source, harness, delivery, human channel).** | Blizzard is an orchestration platform, not a winter- or GitHub-specific tool. Earlier entries that name GitHub or winter (D-005, D-006) are decisions about the reference bindings, not about the platform. | [product/mission.md](../product/mission.md) |
| D-014 | 2026-07-11 | **The baseline is fully autonomous — agents plan, build, verify (review + e2e test), and merge to main; every human gate in the pipeline is opt-in configuration.** | Peer review as a gate is an option, not a requirement. The gate matrix is the HITL→HOTL dial, so it must be configuration, never doctrine baked into code. Supersedes the PR-as-default delivery assumption in earlier drafts. | [product/mission.md](../product/mission.md) |
| D-015 | 2026-07-11 | **Local ask/answer is MVP scope; only the remote fan-out (hub, board, chat bot) is deferred.** | Without an ask channel, every undecidable choice becomes a full human-takeover escalation, defeating unattended throughput. The parking and resume-with-answer machinery is shared with escalation anyway; the remote reach is the genuinely expensive part. Supersedes the earlier draft that deferred the whole protocol to v4. | [product/mvp.md](../product/mvp.md) |
| D-016 | 2026-07-11 | **From v1, every seam is an interface — no component works against a concrete binding.** | Interoperability is the core mechanism, not a later refactor: the reference bindings (winter, GitHub, Claude Code) are the first implementations of the seam interfaces, never a shortcut around them. Deferring the abstraction is how concretes leak in and weld the platform to its first stack. The interface shapes themselves are a discovery deliverable (see open questions). | [product/mission.md](../product/mission.md) |
| D-017 | 2026-07-11 | **Blizzard keeps the name; winter-workflow's `blizzard` build skill (`wf-blizzard`) will be retired.** | The build skill never proved out in practice, and its idea — coordinated multi-agent builds — is being upgraded into this project. Retiring it resolves the naming collision in this project's favor; the retirement itself is winter-workflow work, tracked in that repo. | [README.md](../README.md) |
| D-018 | 2026-07-11 | **Hub auth is configurable, and the first hub ships with none: anyone who can reach the app can do anything, with Tailscale as the network perimeter.** | The hub can run locally or in the cloud, and those need different answers — a cloud hub needs real user identity (OAuth2-class) and permissions, which is complex and explicitly not MVP. Rather than half-design it now, a dedicated post-MVP spike covers identity and permissioning; until then the perimeter is the network. | [design/hub/index.md](../design/hub/index.md) |
| D-019 | 2026-07-11 | **Blizzard is a standalone tool, not a winter extension. The workspace binding is per-runner: an operator prepares a workspace (a plain git worktree setup or a full winter workspace), sets up a runner bound to it, and registers the runner with the hub; agents are spawned into environments that already exist for them — never a fresh clone per agent.** | The workspace is a seam (D-016); packaging blizzard as a winter extension would invert that dependency and weld the platform to one binding. Per-runner binding lets one fleet span simple-worktree users and full winter workspaces. At most, the winter *binding* may someday ship as a winter extension — blizzard itself never does. | [design/runner/index.md](../design/runner/index.md) |
| D-020 | 2026-07-11 | **The app is named `blizzard`; the hub daemon is `blizzard-hub`; the runner is `blizzard-runner`. The broker's verbs live directly under the app (`blizzard ask`, `blizzard status`, …).** | One product, one name, three surfaces. Retires the `wq` working name and resolves the CLI-naming open question; the exact verb set and flags remain open. | [design/runner/contracts.md](../design/runner/contracts.md) |
| D-021 | 2026-07-11 | **Environments are addressed by opaque environment ids, allocated by the workspace integration link — the runner never knows what an environment is, only which ids it holds.** An acquired environment is **clean by contract** (however the provider achieves it); the unit→env binding is a broker fact; the route "task T → runner R, workspace W, env E" flows to the hub. A simple binding mints `feature-{work-tag}` worktrees on demand; the winter binding allocates from its existing environments (alpha, beta, …). | Keeps the runner workspace-agnostic (D-016/D-019): ephemeral-vs-pooled becomes a provider detail behind one contract. Supersedes deterministic env-name derivation as the recovery mechanism — recovery reads the binding fact instead of re-deriving a name. | [design/architecture.md](../design/architecture.md) |
| D-022 | 2026-07-11 | **The hub/runner separation ships in v1: `blizzard-hub` is a v1 component, and the runner speaks the runner↔hub protocol from day one — a solo setup colocates the hub on the runner machine. There is no hub-less topology.** The hub does not replace the runner-local broker: the two hold different things — the broker owns the machine-local fast path (claims, leases, heartbeats, epochs, env bindings), the hub owns the fleet-visible facts (tasks, routes, questions, answers, verdicts, events). The exact facts placement and sync contract is an open question. | The sole motivation is design economy: a hub-less v1 would mean planning a second topology and throwing it away when the hub arrives. With the separation in from the start, local vs remote is a deployment choice (where the hub runs), never an architectural mode. Supersedes the "hub is the broker promoted to a daemon" framing and re-scopes v2 from "introduce the hub" to "run the hub remotely." | [design/hub/index.md](../design/hub/index.md) |
| D-023 | 2026-07-11 | **The runner daemon encapsulates the broker: `blizzard-runner` exposes a local API, the broker sqlite lives inside the daemon, and the `blizzard` CLI is a pure client — local verbs hit the runner's local API, fleet verbs hit the hub's HTTP API. Nothing but the owning daemon ever opens a sqlite file.** | Encapsulation: the CLI modifying sqlite underneath a live daemon means two writers and a schema frozen as a public interface. Behind an API the daemon is the single writer, the schema is a private implementation detail, and the CLI↔runner relationship mirrors the runner↔hub one — one client model everywhere. Trade-off accepted: with the daemon down, local facts are unreadable until systemd restarts it (the facts remain durable in the file). Supersedes D-003's "the CLI is the only doorway to the file" framing — sqlite's rationale (embedded, ACID CAS, no separate DB server) is unchanged. | [design/runner/store.md](../design/runner/store.md) |
| D-024 | 2026-07-11 | **The hub owns the work-source seam: the project-management binding (GitHub issues is one) plugs into the hub, which wraps PM items in its own internal work structure — chunks. Runners acquire work only from the hub, never from the PM system.** | Exactly one component knows the PM layer, so swapping it touches nothing else, and hub-side routing (pinning work to a runner by flag — explicitly not v1) has a natural home. Runners stay PM-agnostic. Supersedes D-005's runner-side SYNC: the PM system still owns the backlog, but the hub mediates it to the fleet. | [design/hub/index.md](../design/hub/index.md) |
| D-025 | 2026-07-11 | **Work moves through a hub-defined workflow graph — the dynamic workflow engine, in v1.** A chunk travels a user-defined, cyclical graph of nodes ("build", "review", …) configured at the hub in YAML; each node carries a prompt and config; a judgement at node exit — v1: worker verdict + deterministic checks (D-009 preserved) — selects the outgoing edge. Delivery is a node; human gates are nodes; the engine plus a default graph ship in v1. | The pipeline, the gate matrix, and the verify step collapse into one mechanism: graph editing *is* the HITL→HOTL dial. Same design economy as D-022 — a hard-coded v1 pipeline would be planned and then thrown away. Re-expresses D-014: the autonomous baseline is the default graph ending in a deliver node. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-026 | 2026-07-11 | **Chunk outputs are artifacts stored at the hub: pointers (a branch name / commit hash, pushed to the forge before submission) or assets (text or arbitrary blobs).** Artifacts accumulate on the chunk and feed later nodes — a review node's findings text becomes the next build pass's fix list. A chunk touching five repos submits five branch pointers. | "Done" needs a durable, fleet-visible output that survives the runner: pointers make git work locatable by anyone, assets carry non-code deliverables (reviews, spikes). Keeps D-012 intact — the hub stores a pointer to code, never the code. Also what makes reassignment possible at all (D-027). | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-027 | 2026-07-11 | **Chunk execution is runner-sticky: the runner holding a chunk advances it through consecutive nodes itself — no re-queue between nodes — applying transition rules the hub owns, and recording every judgement and transition to the hub.** The hub can in principle reassign a chunk between nodes (its artifacts suffice to reconstitute it elsewhere) at the cost of rebuilding the environment; hub-side routing is out of v1. | Env and agent continuity between consecutive nodes is the common case and must be cheap — artifacts already in the worktree, sessions resumable. Authority still lives in one place: the hub defines the rules and holds the record; the runner is an executor of those rules, never their author. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-028 | 2026-07-11 | **"Broker" is retired: the component is the runner store — the runner's embedded database — documented at `design/runner/store.md`.** The fleet-level material the old broker doc carried moves to its real owners: the facts-only/derived-status principle and the PM→hub→store division of truth to architecture.md (they govern both daemons' stores), chunk identity to workflow-engine.md, the CLI verb table to the runner contracts. `epic:broker` becomes `epic:store`. | After D-023 (single owner) and D-024 (queue at the hub) nothing is brokered: one writer, no queue, no mediating role between parties. The name was a fossil of the earlier standalone-file design and over-stated a private implementation detail of the runner as a top-level component. Earlier entries that say "broker" (D-003–D-006, D-020, D-022/D-023) read as the runner store. | [design/runner/store.md](../design/runner/store.md) |
| D-029 | 2026-07-11 | **The roadmap is a sequence of named milestones with stable `milestone:<slug>` ids, replacing numeric versions: `milestone:mvp` (the local fleet) and `milestone:team` (remote reach: remote hub, board, remote ask/answer, chat), then the unscheduled Later pool. A milestone always spans more than one epic; batching, a single epic already parked on the graph-interplay question, moves to Later.** Earlier entries that say v1 read as `milestone:mvp`; v2–v5 read as `milestone:team`. | Numeric versions invited fractional bolt-ons (v1.5) instead of honest re-planning, and single-epic "versions" weren't real releases. Named milestones state what ships and why it's a coherent unit; the id scheme matches `epic:`/`persona:` so scope statements stay traceable. | [product/roadmap.md](../product/roadmap.md) |
| D-030 | 2026-07-11 | **Delivery executes at the hub: the deliver node is the first hub-executed node.** A runner's part ends at branch pointers pushed to the forge (D-026) and the transition into the deliver node; the hub lands the work — merging to main, or opening the pull request where a gate wants one — through an internal **merge queue** that processes one chunk at a time. Epoch fencing moves with it: the hub rejects a submission carrying a stale epoch before it enters the queue. | The delivery lane is fleet-global single-writer state; a machine-local integration lease cannot serialize deliveries across runners (`milestone:team` has several), and the hub already owns the chunk record and never needs to hold code — pointers are merged via the forge. Supersedes the runner-executed deliver-node framing and retires the runner-held integration lease; D-025 (delivery is a node) stands — the executor changes. Merge-queue mechanics (conflicts, PR mode, ordering) are an open question. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-031 | 2026-07-11 | **Telegram is the chat platform for `milestone:team`'s bot.** | Best inline-answer-button UX of the candidates, and the hedge (Telegram vs Slack) was buying nothing — resolving it closes the open question. The chat seam remains a seam; Telegram is its first binding. | [design/ask-answer.md](../design/ask-answer.md) |
| D-032 | 2026-07-11 | **Human gates have two levers: gate nodes in the hub's graph, and runner-side configuration that imposes a human gate on any node by node type. Per-repo gates are dropped.** | The graph gates the whole fleet; runner config lets an individual operator dial their own HITL level without forking the fleet's graph — which is what "per person" always meant in practice. A repo-level gate lever was never actually wanted. Supersedes the "per step, per repo, and per person" phrasing in earlier drafts (D-014/D-025 intact). | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-033 | 2026-07-12 | **Workflow graphs are immutable once created: every edit mints a new graph version, each chunk pins the version it was minted under and travels it to completion, and moving in-flight work to a newer version is an explicit migration operation — never an implicit effect of editing.** | An in-flight chunk can never be stranded at a node its graph no longer has: the transition record always references nodes that exist in the pinned version, so status derivation stays deterministic (D-004) and runner-sticky execution (D-027) never meets a shape-shifted graph. Immutability also makes the pinned version an ordinary durable fact on the chunk. The urgent-fix path (a bad node prompt discovered mid-overnight-run) goes through explicit migration, not live mutation. Migration mechanics — node mapping, chunks at a node with no counterpart, the triggering surface — remain an open question. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-034 | 2026-07-12 | **Chunk migration has two modes. Default — deferred: an in-flight node runs to completion; at the chunk's next transition it moves along its old graph's edge, then immediately re-pins to the target version at the node with the destination node's name. Forced — `migrate --force`: the live claim is revoked by minting a new epoch, the in-flight work is fenced out by the existing epoch check, and the current node is redone under the new version. Migration is its own fact, never a transition: transitions move along edges within one pinned version; the chunk's pinned version derives from its newest migration fact.** | `--force` is the hotfix path — a bad node prompt discovered mid-overnight-run must stop executing now, and revoking the claim reuses the zombie fence (D-007) instead of inventing a second rejection mechanism; the abandoned work is redone by design. Deferred migration respects runner-sticky execution (D-027) and never discards completed work; name-matching is the zero-configuration node mapping. What happens when the target version has no node of that name is an open question. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-035 | 2026-07-12 | **A claim is one node-step execution attempt, minted by the runner in its local store: one claim = one worker tenure (one pid, one session, one verdict), with a fresh fencing epoch per claim (D-007 unchanged). Runner-sticky continuity (D-027) is environment/session/runner continuity, not claim continuity — consecutive node-steps run under successive local claims with no re-queue to the hub. A parked chunk (gate, open ask) holds no live claim; resuming mints a new one, as does every retry attempt.** | The claim maps 1:1 to a worker's lifetime, so the lease/heartbeat/reap machinery guards exactly one execution attempt; fencing gets finer (a zombie from step N cannot write into step N+1's tenure, even on the same machine); parked chunks hold no lease to babysit; and the epoch becomes a unique, monotonic id for a node execution — which the artifact version scheme keys on (D-036). On cross-runner reassignment the hub supplies the chunk's last-known epoch as the floor above which the new runner mints. | [design/concurrency-model.md](../design/concurrency-model.md) |
| D-036 | 2026-07-12 | **Artifacts attach to transitions: a node-step's completion submits its transition and its artifacts as one atomic, epoch-fenced write. A chunk's artifacts form an append-only versioned key-value store keyed `{node}.{artifact-name}.{epoch} → artifact_id`; re-running a node appends, never overwrites; reading an artifact name resolves to the highest-epoch entry. At a human gate, artifacts arrive on a *pending* transition — recorded and visible, judgement not yet rendered. The artifact-level epoch stamp is retired.** | Two separate writes (transition, artifacts) invited the torn state where one landed without the other; as one operation, the transition *is* the artifact commit, and the epoch fence guards a single write — a rejected transition's artifacts never enter the store. Append-only latest-wins matches facts-in/status-derived (D-004), and the key scheme is exactly the selection handle later nodes need. Refines D-026 (artifact kinds and forge-first push unchanged) and D-030's fencing surface: stale epochs are rejected at transition submission. | [design/domain/artifacts.md](../design/domain/artifacts.md) |
| D-037 | 2026-07-12 | **Migration intent and migration record are separate models. A migration request row carries targeted intent (chunk, target version, mode, requester); the migration record is written only at apply time (versions, node ids, `applied_at`), referencing its cause — a request id, or the graph family's standing `auto_migrate` policy. Standing intent lives as that family-level policy — `auto_migrate` (`deferred`/`off`) plus a `migration_target` graph id, usually the family itself, so a family can also be retired by succession into another — never as fanned-out per-chunk rows: a chunk's pending auto-migration is derived (policy `deferred` ∧ pinned version ≠ the target family's latest), and a pending targeted migration is a request with no record referencing it.** | One row serving as both intent and record needed nullable apply-time fields and made "pending" a half-written row instead of a derivation — the split restores facts-in/status-derived (D-004): intent facts in, pending derived, applied history immutable. Family-level policy avoids a publish-time fan-out that races with freshly minted chunks, and covers them automatically. Parked chunks never transition, so they never auto-migrate until resumed — "next convenient time" holds by construction. | [design/domain/work.md](../design/domain/work.md) |
| D-038 | 2026-07-12 | **A node-step's prompting is two-phase. The pre-prompt — the node's base prompt plus the taken edge's addendum — instructs the work; a separate judgement prompt is delivered when the worker declares done, eliciting the structured verdict. D-009's boundary becomes crisp: judgement prompt delivered, no verdict returned → failure. k-forked consensus judgement (fork the session k ways, require aligned verdicts) was considered and deferred — not MVP.** | Building and self-judgement are separate cognitive acts; carrying the verdict rubric inside the work prompt burdens the build and leaves "when is the verdict owed" implicit. The judgement prompt gives the verdict-format question its home — the verdict is the structured answer to that prompt — and adds a third consumer (after answer delivery and the CI loop) of the adapter's missing resume-with-message operation, making that gap clearly required. Forked consensus guards only sampling noise — the forks share one transcript's blind spots — at k× cost: not worth MVP complexity. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-039 | 2026-07-12 | **Graph versions are enabled or disabled, and the flag gates exactly one thing: auto-migration targeting. Auto-migration drifts chunks to the family's newest *enabled* version; a disabled version is never auto-migrated to — and nothing else changes: chunks pinned to it continue, explicit migration requests may still target it, and the immutable definition is untouched. Versions mint enabled. Enabled-ness is an operational fact beside the version, not part of it: enable/disable facts append, the flag derives (D-004).** | A bad version (broken prompt, wrong topology) must be removable from the rollout path instantly, without authoring a newer version or rewriting history. Keeping the semantics to that single effect stops the flag from growing into a second lifecycle system, and keeping it outside the definition preserves D-033 immutability. | [design/domain/graph.md](../design/domain/graph.md) |
| D-040 | 2026-07-12 | **There is no graph family. Graphs are standalone immutable entities: every edit mints a new graph with its own unique `graph_id`, correlated with its predecessors only by *name* — exactly as nodes are correlated across graphs (D-034). Any graph may migrate to any graph, provided the node mapping gets the chunks over. Each graph carries its own standing drift intent — `auto_migrate` (`deferred`/`off`) plus `migration_target` (a `graph_id`), honored only while the target is enabled (D-039). Lineage is emergent from `migration_target` chains, and a chunk follows a chain one hop per transition — no transitive resolution.** | The family was a second grouping mechanism on top of what the pointer already expresses: "version of" is just "migrates to" between similar shapes, and succession into a renamed or restructured workflow was already cross-family migration — one concept (graph + pointer) covers both. One-hop-per-transition drift needs no chain resolver and self-corrects as pointers change. Earlier entries' "graph version" reads as the graph; "the family's newest enabled version" reads as the graph's `migration_target` (while enabled). | [design/domain/graph.md](../design/domain/graph.md) |
| D-041 | 2026-07-12 | **Nodes carry no `type` field. The runner-side human-gate lever (D-032) selects nodes by *name*. What type appeared to encode is structural: a gate is a node whose judgement is rendered by a person (`judged by: human`), hub execution is `executor: hub` — engine behavior never hangs off a classification label. Free-form tags were considered and deferred.** | The field's only real consumer was gate-config selection, and operators think in the node names they see in their graphs ("gate `deliver`") — a name selector spans graphs by convention, which is acceptable; a parallel controlled vocabulary was a second classification inviting engine coupling and inferable-looking redundancy with prompts and structure. D-032's "by node type" reads as "by node name." | [design/domain/graph.md](../design/domain/graph.md) |
| D-042 | 2026-07-12 | **Judgement outcomes are reified as Choice entities. A node's judgement spec declares its choices — `choice_id` minted at graph mint (the exact reference), short `name` unique within the node (the author-facing correlator), label/description for humans; edges carry `choice_id`, so "every outcome has an edge" is id resolution at mint, never string matching. The elicitation tail is engine-generated from the choice set and appended to the authored judgement prompt — "Here are your available options: … select exactly one and output it as `<Choice>{name}</Choice>`" — uniform across all nodes and graphs; a missing or unparseable `<Choice>` is a missing verdict (D-009). Gates consume the same model: a gate's choices are what the board and chat bot render as buttons. Choices are per-judgement-spec, never a global registry — names correlate, ids are exact (the D-040/D-041 philosophy).** | The outcome vocabulary lived twice — the judgement's outcomes and the edge keys, coupled by string equality — so a typo was a runtime routing failure; as ids it fails the mint. Engine-owned elicitation removes per-author verdict-format drift and structures the verdict's selection half by construction, narrowing the verdict-format open question to the assessment payload and transport. One Choice model serves worker elicitation, human gates, and edge routing. | [design/domain/graph.md](../design/domain/graph.md) |
| D-043 | 2026-07-12 | **Runner control is declarative state, not a directive queue. The registry entry carries a `paused` flag set via `PATCH /runners/{id}`; pause/resume facts append and the flag derives (D-004, the D-039 pattern). The runner reads its own state over its outbound pull and adheres to it — D-012 unchanged: state is pulled, never pushed into the dev box. The Directive model and `POST /runners/{id}/directives` are retired; post-MVP controls (routing pins) take the same declarative shape.** | A pause is a condition to hold, not a message to process once: desired state on the resource is idempotent under replays, restarts, and missed deliveries, and the runner's obligation is legible from the registry row itself — where a queued directive needs consume-and-ack machinery and lets what was sent drift from what the runner believes. | [design/domain/fleet.md](../design/domain/fleet.md) |
| D-044 | 2026-07-12 | **Claim facts are fleet-visible: each claim mint (chunk, epoch, runner) is reported to the hub, which derives the chunk's latest epoch, fences every state-advancing write on it (D-007/D-036), and supplies the epoch floor on reassignment (D-035). Claims are still minted at the runner, and the runner store remains authoritative for the machine-local execution facts — leases, heartbeats, pids, env bindings; "never leaves the box" names those, not epochs.** | The hub cannot reject a stale epoch, nor supply a reassignment floor, without holding epoch facts — three documents denying it holds them contradicted the fence the whole design leans on. Refines D-022's division of truth: the fast path stays local, but the fence's input is fleet-visible by necessity. | [design/architecture.md](../design/architecture.md) |
| D-045 | 2026-07-12 | **A gate parks as an open Decision, not a pending transition — and the runner authors both outcomes. The holding runner submits a node-step's completion as either a Transition or a Decision row (chunk, node, the judgement's choice set, artifacts — one atomic epoch-fenced write, D-036's atomicity intact), and the hub validates which is legal: at a human-judged node a submitted transition is rejected — human signoff required — while a runner may submit a Decision in place of a transition for *any* node, which is exactly the runner-config gate mechanism (D-032). The chunk derives `waiting_on_human` from an unresolved decision exactly as from an open ask, and pending is derived — a decision no transition references (the D-037 pattern). Resolution — a person picking a choice (D-042: the board's buttons) — is recorded at the hub first-write-wins like an answer; the holding runner picks it up over its outbound pull and records the ordinary, fully-formed Transition referencing the decision (D-027 intact: the runner advances the chunk). The choice set can later grow override options (route to an arbitrary node past the graph's edges; stop the chunk) without touching the transition model. D-036's "pending transition" reads as "open decision."** | A transition's `to_node_id` is selected by its judgement, so a judgement-less transition was structurally unsatisfiable — a half-written row, exactly what D-037 abolished for migration. Runner-authors/hub-validates keeps one transition author per executor and turns the gate dial into data, not a second write path; the decision row reuses the parking shape the corpus already has (durable row in, chunk parked, resolution resumes) and gives the board a concrete thing to render. | [design/domain/work.md](../design/domain/work.md) |
| D-046 | 2026-07-12 | **`milestone:team` is renamed `milestone:centralized-hub`: enable the control plane beyond the local environment, with spokes fanning out of the hub — remote runners, the board, the chat bot, remote ask/answer. `epic:team` (multi-operator policy) is untouched and stays unscheduled.** | The milestone's content is topological — where the hub runs and what connects to it — not organizational; naming it for the hub-and-spoke shape says what actually ships, and frees "team" for the epic that really is about people. The two sharing a word invited conflation. Earlier entries' `milestone:team` reads as `milestone:centralized-hub`. | [product/roadmap.md](../product/roadmap.md) |
| D-047 | 2026-07-12 | **Work enters the hub by explicit, id-addressed ingestion — never a label sweep. Ingesting a PM item stores a pointer — (provider, base URL, remote id) — and mints a chunk by default, one per item (a chunk carries an array of pointers; grouping arrives with batching). Item contents are read pass-through: anything that needs the guts (the build node included) fetches them via the hub's API, and the hub calls the PM system with its own machine credentials (per-vendor app registration), returning the body in whatever format the vendor provides. Chunks are ephemeral: any unclaimed chunk may be discarded, and re-ingesting the same pointer mints a fresh chunk. The MVP writes nothing back to the PM item — write-back is a future idea, deliberately unexplored.** | A sweep contract demands enumerate/subscribe/label-taxonomy/ordering from every PM binding — thousands of irrelevant items in a real tracker, and a fat seam that makes the second binding a rewrite. A pointer plus pass-through keeps the hub's deterministic core PM-agnostic (the per-vendor binding inside the hub owns auth and call recipes — the hub *is* the abstraction), keeps PM credentials off runners and workers entirely, and keeps the PM item the durable referent, so chunks stay a cache and a workflow position, never a competing backlog. Selection — deciding which items are fleet-ready — moves outside the hub, to humans or agents ingesting specific ids, and queue ordering becomes a hub-side property. The body's consumers are LLMs, so format normalization is unnecessary. Refines D-024 — the hub still owns the work-source seam and runners never touch the PM system; D-024's "reflects state and completion back" is out of the MVP. | [design/hub/index.md](../design/hub/index.md) |
| D-048 | 2026-07-12 | **The hub ships a web front in the MVP: `epic:board` slices, and its local slice — the hub-served web app — lands in `milestone:mvp` with fleet observability (chunks, node histories, artifacts, questions, runners, merge queue) plus two operator controls: prioritizing the ready queue and grouping unclaimed chunks (merging their PM pointers into one chunk). The remote slice — PWA reach, viewer/operator roles, auth — stays at `milestone:centralized-hub`. Manual grouping does not unpark `epic:batching`: automated packing and the LLM planner remain Later, and solo execution (one env, one agent per chunk) holds.** | `blizzard status` answers "what is the fleet doing," but prioritizing and grouping are queue-shaping acts — clumsy as CLI verbs, natural on a visual surface — and D-047 made both hub-side by construction (ordering is a hub property; a chunk carries an array of pointers). The web app is a pure client of the hub's existing HTTP + SSE surface, colocated and authless behind Tailscale (D-018), so pulling it forward costs no architecture. Supersedes the MVP cut of `epic:board` ("visibility is `blizzard status`") and D-047's "grouping arrives with batching" — *manual* grouping is a web-app control; automated grouping is what batching remains. | [design/hub/web-app.md](../design/hub/web-app.md) |
| D-049 | 2026-07-12 | **The runner-local, per-node-step record is renamed *lease*; *acquisition* is the hub granting a chunk to one runner; *claim* is retired as a standalone term — it wrongly implied the local record arbitrates rival claimants. Work-assignment contention lives at one boundary only — the hub. Acquisition (hub → runner, D-024) is the sole point where rival claimants compete for a chunk, because separate runner machines are the only rivals that exist; the hub grants each chunk to exactly one runner, and that is where fleet exactly-once is upheld. The runner-local claim is a single-writer lease, not a contended CAS: a runner is the one writer on its machine (D-023), so leasing a node-step to a worker slot is crash-safe, idempotent bookkeeping with no race to lose. A reaped-but-still-alive worker is neutralized by the hub-checked fencing epoch (D-007/D-044), never by runner-local mutual exclusion — so there is deliberately no runner↔agent lease/fence protocol: the supervisor is the OS parent and the sole hub-writer, it reaps the dead agent's whole process tree and reconciles its own pid list atomically, and a zombie agent has no path to hub truth to fence. Reusing the environment across a reaped→respawned agent is the workspace provider's concern (D-021), not the runner's.** | The CAS-as-arbitration framing implied intra-runner contention that does not exist (one daemon, one writer, no rivals) and misattributed the fleet's exactly-once guarantee to the runner store when it belongs to hub acquisition (D-024) plus the epoch fence (D-007/D-044). Naming the two tiers apart dissolves the "claim against whom?" confusion and scopes any agent-level lease as YAGNI. Refines D-003's "atomic CAS claims" — sqlite's conditional write buys crash-safe idempotency and queryable derived status, not race arbitration; D-005's "the local store owns who is working now" stands, now without the contention connotation. Earlier entries and docs that say "claim" for the runner-local record read as "lease"; where they meant the hub grant, read as "acquisition" (the D-028/D-046 supersession pattern). | [design/concurrency-model.md](../design/concurrency-model.md) |
| D-050 | 2026-07-12 | **The harness adapter interface gains a fourth required operation: automated resume-with-message — `resume(env, session_id, message) -> pid`.** Its three consumers were already named: the judgement prompt (D-038), answer delivery, and the CI feedback loop. | The gap was interface-shaped, not capability-shaped: all three target harnesses support it today (`claude -p --resume <id>`, `opencode run --attach`, `codex exec resume <id>`), so requiring it costs nothing, while an optional-with-fallback shape would complicate every caller for a harness that doesn't exist yet. | [design/harness-adapters.md](../design/harness-adapters.md) |
| D-051 | 2026-07-12 | **Migration's unmapped-node case: the chunk falls back to the target graph's entry node.** When the target graph has no node matching the destination's name, the chunk re-enters the target at its entry node and redoes the work from the top. | A simple universal rule that needs no declarative-map machinery: name-matching stays the zero-configuration common case (D-034), and the bounded rework on a name miss is an acceptable price. Declarative old→new maps and stay-on-the-old-graph-until-mappable were considered and rejected as heavier. | [design/domain/graph.md](../design/domain/graph.md) |
| D-052 | 2026-07-12 | **A parked-at-gate chunk's open Decision surfaces through both the CLI and the web app from day one, backed by the same hub route: a `blizzard` verb lists and resolves open decisions, and the board renders them with resolution buttons (D-042's choices).** | Decisions are already hub rows (D-045) and the web app already ships in the MVP (D-048), so both surfaces are thin clients of one route; a CLI-only MVP would undercut the board's operator role for no savings. | [design/hub/web-app.md](../design/hub/web-app.md) |
| D-053 | 2026-07-12 | **The node envelope is pure-LLM: every artifact is presented in the composed prompt — pointer artifacts as their git commit hash and feature branch name — and the agent sets up its own workspace from them. Agents manage environments, not the other way around; any pre-materialization is a configurable property of the runner↔workspace seam, never engine behavior.** | The agent is the component best equipped to decide what to check out and where, so baking materialization into the runner would fatten the workspace seam for no gain; providers that want to pre-materialize still can, behind the seam. | [design/runner/environments.md](../design/runner/environments.md) |
| D-054 | 2026-07-12 | **Session continuity is per-node graph configuration with resume as the default: a node may declare a fresh session (cold eyes); otherwise the chunk's prior session is resumed.** | D-027 makes continuity cheap and it is the common case; fresh-per-node is exactly what review-style nodes want, and the choice belongs to the graph author, not a runner heuristic. | [design/domain/graph.md](../design/domain/graph.md) |
| D-055 | 2026-07-12 | **"Declares done" is exit: any verdict-less exit without an open ask receives the judgement resume (D-038), and the judgement reply — or its absence or unparseability (D-009) — is what distinguishes done from crash.** No `blizzard done` verb, no sentinel output, no exit-code split. | Exit is the only signal every harness emits reliably without agent cooperation, and the judgement phase already exists as the disambiguator: a crashed session simply fails its judgement and the attempt counts against the node's retries. | [design/runner/loop.md](../design/runner/loop.md) |
| D-056 | 2026-07-12 | **The verdict's assessment payload travels via harness-native structured output (`--output-format json`, `--output-schema` — which works on resumed runs), normalized by the adapter's `verdict()`. No `.fleet/result.json` file convention.** | Harness-enforced structure beats trusting the agent to write a file, and one transport keeps per-harness variance localized in the adapter — parsing, not deciding, so D-008's adapters-stay-dumb holds. | [design/harness-adapters.md](../design/harness-adapters.md) |
| D-057 | 2026-07-12 | **The hub's merge queue is strict FIFO, one chunk at a time: a chunk's branch pointers land across all its repos before the next chunk starts. No train batching in the MVP.** | Cross-repo atomicity per chunk is the property that matters; train batching is a throughput optimization with real complexity that can be added later without changing the model. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-058 | 2026-07-12 | **A merge/rebase conflict at the deliver node routes the chunk to a merge workflow by convention: if a graph named `merge` exists (the name is configurable), the chunk migrates there — its own prompts for merging, review, and redelivery; if none exists, the chunk falls back to the entry node of its own graph (the D-051 rule). A graph may also carry its own `merge` node routing to deliver — the deliver node deliberately has two different entrypoints.** | Conflict resolution is agent work, and the graph is the mechanism for agent work: a conventional named graph gives it a home without bloating every graph, while the in-graph merge node lets authors who want it inline keep it inline. Parking conflicts as needs-human would defeat unattended operation for a routine occurrence. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-059 | 2026-07-12 | **PR-mode merging is capability-gated: both the hub and humans may merge, but the hub merges only where the delivery binding exposes a merge-via-API capability — where it doesn't, the hub cannot merge. Externally-merged PRs should ideally be detected and ingested so the chunk completes; the detection mechanism is a new open question.** | Whether a merge API exists is a per-binding fact, so the seam must express it as a declared capability rather than the engine assuming it; manual merges will happen regardless, so detection is a real need — deferred as its own question rather than half-designed here. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-060 | 2026-07-12 | **No fencing at branch-push time: the pointer artifact's commit hash is authoritative — the feature branch name serves only to detect commits ahead of the recorded hash. A zombie clobbering a branch ref is accepted risk; stale-epoch submissions are already rejected at the hub (D-030/D-036).** | Because the artifact pins the exact hash, a clobbered ref can never redirect what gets merged — the worst case is lost (redone) work, never wrong work landing — so per-epoch branch names and force-with-lease buy little for the ref hygiene they cost. | [design/domain/artifacts.md](../design/domain/artifacts.md) |
| D-061 | 2026-07-12 | **One binary, and every CLI verb names its target system: `blizzard` ships as a single executable whose verbs group under the two client nouns — `blizzard runner <cmd>` against the runner's local API, `blizzard hub <cmd>` against the hub's HTTP API — with no bare verbs; the daemons are invocation personalities launched by a `host` verb under each noun (`blizzard runner host`, `blizzard hub host`), and D-020's hyphenated daemon names (`blizzard-runner`, `blizzard-hub`) survive as aliases/entry points for systemd and `ps` legibility. The ask/answer pair deliberately spans the nouns — `blizzard runner ask` records the local ask fact, `blizzard hub answer` writes the answer where the question row lives. Supersedes D-020's "verbs live directly under the app"; resolves the one-binary-or-three open question.** | A CLI that targets two systems must say which one in the command — dispatch is spelled, not memorized. One artifact makes CLI↔daemon skew on a machine structurally impossible (the hook path always speaks its daemon's version), ships the shared facts schema and derivation queries in one place (separate codebases would recreate the adapter-drift hazard internally), and deploys as one install — the consul/ollama/opencode serve pattern, with `host` as blizzard's verb for it. Two constraints survive the packaging choice: the runner↔hub wire protocol carries a version field (cross-machine skew is inevitable), and the hook path (`blizzard runner heartbeat` on every tool call) must stay milliseconds-cheap — which weighs on the implementation-language choice. | [design/cli.md](../design/cli.md) |
| D-062 | 2026-07-12 | **The workspace provider is allocation-stateless, and the runner store is the single durable truth for environment holds: `acquire(chunk_id, count, held_ids)` picks from the provider's static pool minus the held set the runner passes in, and returns `(env id, working directory)` pairs — the workdir is a provider-returned fact, never a path the runner computes. Idempotent re-acquire is answered from the runner's own binding facts; the provider sees only genuinely-new allocations. Provider-native state (a worktree's existence, winter's env-index) is substrate, not allocation truth; drift reconciles one way, runner → provider.** | The chunk→env binding fact is already written transactionally with the lease mint and REAP already releases it — "held" is derivable from facts the runner owns, which is D-004 applied to allocation. A provider-owned durable table would be a second source of truth with a real desync window between the two writes, and would push crash-safe storage onto every BYO binding; statelessness makes a bring-your-own provider a small script. The mid-acquire crash (provider cleaned an env, binding fact never written) is harmless by construction: the env stays free per the runner's facts and reset-on-acquire absorbs the orphaned clean. Supersedes environments.md's "the provider keeps its own allocation table" sketch. | [design/runner/environments.md](../design/runner/environments.md) |
| D-063 | 2026-07-12 | **The runner tells the worker which environments it holds: a machine-local spawn preamble composed adjacent to the hub's node envelope — the held env ids with their working directories — plus `BLIZZARD_ENV_IDS` in the spawn process environment for hooks and tooling.** | The node envelope is hub-composed at PULL, but env ids exist only after FILL acquires — only the runner can say them, so they are a runner-side preamble, never an envelope field. Presenting the ids completes D-053: the id is a name grant, and under a lazy binding the agent can materialize the environment itself from the preamble plus the presented artifacts. Rides the existing spawn-injected-identity pattern (`BLIZZARD_LEASE_ID`/`BLIZZARD_SESSION_ID`). | [design/runner/environments.md](../design/runner/environments.md) |
| D-064 | 2026-07-12 | **The workspace provider is a capability slot: the reference bindings (plain git, winter) compile into the one `blizzard` binary, and a bring-your-own provider is an invoked executable behind a versioned exec protocol (argv/stdio, JSON out) — arbitrary code, deliberately.** | Mirrors winter's own `[capabilities]` pattern and honors D-016 (every external system behind a seam with reference bindings) without breaking D-061's one-artifact deployment for the common case. The contract is small enough — acquire, release — that the exec seam is cheap to define now, and D-062's statelessness is what makes BYO tractable: the engine holds the durable truth, so external providers stay dumb. | [design/runner/environments.md](../design/runner/environments.md) |
| D-065 | 2026-07-12 | **Externally-merged PRs are detected by hub-side polling, with an on-demand check route, and webhooks later as pure acceleration: the hub polls the delivery binding's PR-state read for chunks parked awaiting an external merge (a slow tick over a small set), and `POST /chunks/{id}/check-delivery` triggers the same check immediately. Any terminal PR state completes the chunk — merged or closed-without-merge, the chunk moves to delivered; the recorded fact distinguishes the two and carries the actually-landed commit where one exists. A future inbound webhook merely triggers the immediate check — polling stays the source of truth.** | A colocated hub behind Tailscale (D-018) is unreachable from the forge, so inbound webhooks cannot be load-bearing; and a parked chunk generates no submissions, so check-at-submission alone would never fire. Treating close-without-merge as completion follows from the forge disposition being a human's explicit act — parking the chunk instead would demand human attention twice for one decision. Resolves D-059's detection question. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-066 | 2026-07-12 | **The runner holds a chunk's environments until the hub reports the deliver node's terminal outcome — including across a PR-mode wait for an external merge. A landed (or externally closed, D-065) delivery releases them; a conflict routes back through the graph (D-058) into the same warm environments.** | One rule with no release-on-park/re-acquire machinery to build in the MVP, and the warm environment is exactly what makes D-058's conflict routing cheap for a routine occurrence. The capacity cost of a long external-merge wait is accepted, and shortened in practice by D-065's immediate-check route; release-on-park with deterministic re-acquire (the branch pointer's hash makes reconstruction safe, D-060) is the known revision if environment starvation shows up in fleet operation. | [design/runner/loop.md](../design/runner/loop.md) |
| D-067 | 2026-07-12 | **The fact/event vocabulary is canonical in the domain model: seven derived chunk statuses (`ready`, `running`, `delivering`, `waiting_on_human`, `needs_human`, `stopped`, `done`) computed from named facts with an explicit first-match precedence, each fact owned by exactly one store — hub-landed, runner-minted-and-flushed (`lease.minted`, `escalation.recorded`, `answer.delivered`, takeover facts), or machine-local-forever. Fact names double as wire event names on the SSE stream and the batched push. Three edges settle with it: `delivering` derives from the newest transition entering a hub node; an escalation closes by derived supersession — a later lease fact from requeue or takeover, never a resolution fact; and `stopped` is a terminal operator fact (`POST /chunks/{id}/stop`), legal any time after acquisition, artifacts and history retained, terminality fencing all later writes.** | D-004 promised derived status but left the fact list improvisable per implementer — the vocabulary is the contract that keeps both daemons' derivations agreeing. `delivering` names the D-066 hold window honestly (a days-long external-merge wait is not "running"); supersession reuses the pending-is-derived pattern (D-037) and cannot desync, where a forgettable resolution fact could; `stopped` fills the hole where a `needs_human` chunk had no legal abandonment path — discard is unacquired-only (D-047). `stalled` stays a runner-local derivation because the worker heartbeat never leaves the machine (D-012's trust boundary applied to telemetry). | [design/domain/events.md](../design/domain/events.md) |
| D-068 | 2026-07-12 | **The runner's local API speaks HTTP over a unix domain socket, and there is no degraded read path while the daemon is down. HTTP semantics keep the resource-oriented route table as written; the socket lives under the runner's state directory with filesystem permissions as access control; localhost TCP is an opt-in flag, so by default the runner listens on no TCP port at all. Daemon down → the CLI prints a daemon-not-running diagnostic; raw sqlite inspection is documented break-glass forensics, never a supported surface (D-023 intact). Hook failures split soft/hard: a heartbeat failure is soft — it never breaks the worker's tool call, worst case a reap — while an ask failure is hard, because the worker must not exit believing an unrecorded ask was recorded (the park-vs-death discriminator).** | Both transports are sub-millisecond — the hook path's real cost is CLI process spawn, which no transport fixes — so the decision falls to surface area: no listening port makes "anything that pushes into the box" structural (D-012), and no port registry per machine is needed. A CLI that reads the sqlite directly would reopen D-023 for a window that systemd restart mostly covers. | [design/runner/api.md](../design/runner/api.md) |
| D-069 | 2026-07-12 | **Runner→hub is store-and-forward always: every hub-bound fact enters the outbound buffer at mint, stamped with a per-runner monotonic sequence number, even when the hub is reachable; a single FIFO flusher drains it to the hub, which keeps a per-runner high-water mark — a fact with seq ≤ mark is already-applied and acked without re-applying, and a semantic rejection (a stale-epoch transition) still advances the mark: rejection is an outcome, not a delivery failure.** | One code path whether the hub is up or not — the outage path is the normal path with a bigger backlog, never a rarely-exercised branch that must be perfect. The monotonic seq gives at-least-once delivery with exactly-once effect plus free gap detection, and makes D-044's per-runner FIFO structural: the buffer is the only path, so ordering cannot be violated by a fast path that skips it. | [design/runner/store.md](../design/runner/store.md) |
| D-070 | 2026-07-12 | **Runner liveness is a dedicated heartbeat route — `POST /runners/{id}/heartbeats`, mirroring the local worker heartbeat's shape — refreshing the registry's `last_seen_at`, from which online/offline derives against a staleness threshold. The SSE subscription never counts as liveness. Cadence and threshold are constants (much slower than the worker tool-call heartbeat, which never leaves the machine — D-067), tracked with the other constants.** | An explicit, named signal is legible on the wire and in logs where an empty event batch doing double duty is not; dead SSE connections linger silently behind NATs and proxies, faking liveness exactly when it matters. | [design/domain/fleet.md](../design/domain/fleet.md) |
| D-071 | 2026-07-12 | **The workflow YAML authoring schema is canonical: fused choice entries — each judgement outcome is one entry carrying its description, its `to` destination, and optionally its `prompt_addendum` — with `done` as the reserved terminal target and `escalate` as the only retry-exhaustion target; hub-executed nodes have machinery-defined outcomes with default routing (deliver: `landed → done`, `conflict →` the D-058 convention) that an authored entry overrides per outcome; mint-time validation splits errors (one entry node, every `to` resolves, judgement-prompt presence matches the judged-by kind, every file reference inlines) from warnings (unreachable nodes, no path to `done`); no acyclicity check — cycles are intentional, bounded by retries.** | Fusing choice and edge makes "every choice has an edge" unrepresentable to violate instead of a rule to check, and reads as the author's actual mental model: outcome → meaning → destination. Default routing on hub nodes keeps the common graph free of machinery detail while leaving inline conflict routing (D-058's second entrypoint) one entry away. The fusion is authoring surface only — reified Choices and Edges with exact ids still mint underneath (D-042). | [design/hub/graph-schema.md](../design/hub/graph-schema.md) |
| D-072 | 2026-07-12 | **Transition submission is the synchronous advancement checkpoint: the runner submits a node-step's completion (judgement choice, check results, artifacts) through the outbound buffer (D-069 intact), and the hub's apply-response — the transition recorded and any pending migration applied atomically in the same write — carries the chunk's next node envelope back, which is what lets the runner continue in place. The runner holds no graph definitions: envelopes are hub-composed at PULL and at each apply (extending D-063), and a migrated chunk's swap is discovered in the returned envelope's graph/node ids, never inferred. Deferred migrations therefore apply at transition ingestion; verdict-less failures (D-009) retry runner-locally from the envelope's own retries config, with no round trip.** | The hub already owns workflow state (D-022), so letting it apply the edge and the migration in one atomic write eliminates the apply race entirely — there is no window where runner and hub disagree about the pin. The cost is named and accepted: advancement needs hub reachability, so during an outage chunks finish their current node and wait at the boundary (workers mid-node unaffected) — milliseconds-cheap in the colocated MVP (D-022), and a legible degradation for a remote hub. Refines D-027: the chunk still never re-queues and the holding runner still drives it — the hub answers, the runner acts. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-073 | 2026-07-12 | **Runner-side gate configuration is runner-local declarative config: a list of node names (`gates = ["deliver", …]`) in the runner's own config, matched by name across all graphs (names are the cross-graph correlator, D-041), reloaded on tick. Effective gating is the union of graph gates and runner gates: for a gated node the runner submits a Decision instead of a transition (D-032/D-045), whoever asked.** | D-032's point is an individual operator dialing their own HITL level without forking the fleet's graph — hub-held per-runner gate state would turn a personal dial back into fleet state. Union composition needs no precedence rules: a gated node is gated. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-074 | 2026-07-12 | **The PM ingestion surface: `blizzard hub ingest` fronts `POST /chunks` (verb + route, D-061 symmetry), and the agent-native ingest flow — an LLM with the PM system's own tooling supplying specific ids — is documented as a pattern, not shipped. The pass-through read (`GET /chunks/{id}/pm-items`) exposes each item's body **and comment thread**, vendor-native format, with the pointer and `fetched_at`; attachments and linked items wait. Per-vendor credentials are hub config — a token per provider section from an env var or file reference; OAuth/app registration waits for the identity spike (D-018). Discard mechanics resolve against D-067: `DELETE /chunks/{id}` stays unacquired-only and is real deletion — nothing to retain, the PM item is the durable referent (D-047) — while anything with history is `stopped` instead (terminal, artifacts retained); in the authless MVP anyone with hub access may do either.** | A shipped canned flow would drag prompt maintenance for one vendor into the hub MVP; comments are where acceptance clarifications actually live, so body-only would hide the real spec from workers; and the discard/stop split needs no retention policy because the two verbs partition cleanly on whether history exists. | [design/hub/index.md](../design/hub/index.md) |
| D-075 | 2026-07-12 | **Identity: a PM pointer is `{provider, url}` — the provider tag naming the binding plus the item's canonical web URL, from which the binding derives its API calls (refines D-047's triple: `base_url` and `remote_id` fold into the URL). Every hub-minted id is a prefixed ULID — `ch_` chunks, `gr_` graphs, `nd_` nodes, `qs_` questions, `dc_` decisions, and so on for every hub entity.** | The canonical URL is universal, self-describing, and board-legible without reassembly, while the explicit provider tag survives self-hosted vendors where host inference breaks. Prefixed ULIDs are type-evident in logs, lexically creation-ordered (listings need no separate sort key), and copy-pasteable in the CLI; one convention ends per-entity bikeshedding. | [design/domain/work.md](../design/domain/work.md) |
| D-076 | 2026-07-12 | **Vocabulary: "chunk" is canonical for the unit that travels the graph and that a runner acquires and holds; a "PM item" is the vendor's item behind a pointer; the planner's "execution unit" is retired — a batch is one chunk carrying many PM pointers, which is what grouping already produces (D-047/D-048).** | Three names for two concepts was drift, not nuance; retiring "execution unit" now costs one glossary edit, while the parked batching question inherits a single vocabulary instead of a reconciliation. The planner docs keep their parked banner and get rewritten in chunk terms when batching lands. | [glossary.md](../glossary.md) |
| D-077 | 2026-07-13 | **MVP judgement is the worker's verdict alone: the worker runs the node's checks itself, in-session, and folds their results into the verdict it renders.** Planning-flavored stations generate a verification checklist as an asset artifact (the winter-harness verifiability-matrix pattern); later nodes verify against it before judging. The `checks` field survives in the schema as worker-delivered instructions; engine-executed deterministic checks are post-MVP. | The verdict×checks combination rule (worker says pass, checks say fail → which edge?) was undefined, and engine-run checks need execution-context design (per-repo cwd, timeouts, output capture) that buys little while the worker can run the same commands in-session — where their output is context the judgement can actually use. In the MVP there is nothing to combine: the verdict is the judgement. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-078 | 2026-07-13 | **Retries count execution-attempt failures only — a crash, a verdict-less or unparseable exit, a reap, a context-exhausted restart. A judged failure choice traversing an edge never consumes a retry.** Cycle bounding (max-visits per node) is future scope, not MVP. | Retries guard against LLM error cases; a judged fail is the graph working as designed — the review-fail loop is the point (D-025), and charging it against a cap of 2 would escalate healthy iteration. Endless build↔review ping-pong is a real hazard, deliberately deferred to a later max-visits mechanism. | [design/runner/loop.md](../design/runner/loop.md) |
| D-079 | 2026-07-13 | **Hub nodes execute under the hub's coordinator — a singleton, runner-like internal component: it acquires the chunk's hub node-step, executes it (the deliver node first, D-030), and records the exit transition itself, minting the step's lease and epoch from the chunk's same epoch sequence.** Hub-node transitions are thereby fenced and authored exactly like runner ones — the transition's author field carries the coordinator where it would carry a runner id. Post-MVP sharding into multiple coordinators is possible but not foreseen. | Transitions carried `runner_id` and a lease epoch, but the deliver node had neither — hub-node exits had no author, no epoch, and no defined writer. A singleton coordinator gives them all three without introducing hub-side concurrency, and keeps one transition model for the whole graph. | [design/hub/index.md](../design/hub/index.md) |
| D-080 | 2026-07-13 | **Acquisition is claim-by-route, environments first: FILL peeks the hub's ready queue read-only, acquires the chunk's environments from the workspace provider, then claims by POSTing the complete route — chunk, runner, workspace, env ids — in one write; the hub accepts exactly one claim per chunk, and a 409 is race-second-place: release the bindings, do nothing, the next tick peeks fresh. The accepted claim's response carries the first node envelope.** PULL stops acquiring; it remains the fact channel. | The Route model carried `environment_id` but routes were born at PULL, before FILL acquired environments — the two documents could not both be true. Claiming after env acquisition makes the route complete at birth, keeps hub-side ordering authoritative (the peek returns the hub-ordered head), and makes multi-runner racing legible: D-024's fleet exactly-once is upheld as claim validation. | [design/runner/loop.md](../design/runner/loop.md) |
| D-081 | 2026-07-13 | **Hub configuration names a default graph — by name, resolving to the newest enabled mint of that name — and ingestion pins every new chunk to it. The first graph is provisioned manually via `POST /graphs`. Per-chunk graph selection at ingest, and re-pointing an existing chunk, are post-MVP.** | Chunks pin a graph at mint (D-033) but nothing said which; a config-named default is the smallest mechanism that closes the gap, and name-resolution reuses the existing correlator convention (D-040). | [design/hub/index.md](../design/hub/index.md) |
| D-082 | 2026-07-13 | **A lease spans one node-step = one session, never one pid: the spawn, the judgement resume, and the answer resume all run under the same lease — and a parked chunk (open ask, open decision) keeps its lease, dormant, with the reap clock stopped. Reap ends an attempt — kill, then retry or escalate per the retries config — it does not by itself forfeit the chunk or its environments; a retry is a new attempt: new session, new lease, fresh epoch (D-007 unchanged). Takeover voids a parked lease; requeue mints fresh.** Supersedes D-035's one-pid phrasing and its parked-chunks-hold-no-lease rule; D-035's per-attempt scope and epoch minting stand. | Two-phase prompting (D-038) made "one pid" false the day it landed, and ask-and-exit resumes the same session — the same tenure — so parking-releases-the-lease forced identity churn the design never wanted. Reap-as-retry, not reap-as-forfeit, is what the loop already did; the lease model now says so. | [design/concurrency-model.md](../design/concurrency-model.md) |
| D-083 | 2026-07-13 | **Environment holds are chunk-tenure-scoped: bound when the route is claimed (D-080), held across parks, escalations, the judgement phase, and delivery, and released only when the chunk leaves the runner — terminal outcome, stop, or detach (D-088).** Subsumes D-066: hold-until-delivery-outcome becomes one case of the general rule. REAP kills processes and expires attempt leases; it frees environments only when the tenure ends. | The binding "riding the node-step lease" contradicted every long park — an ask answered next morning must resume in the same worktrees, a takeover lands in them, and delivery already held past the last lease (D-066). Tenure-scoped holds are the rule all those cases silently assumed; the capacity cost is the one D-066 already accepted. | [design/runner/environments.md](../design/runner/environments.md) |
| D-084 | 2026-07-13 | **Communication is strictly layered: worker ↔ runner, runner ↔ hub, hub ↔ PM — never crossing a layer. PM pass-through reads reach the worker through a runner local-API proxy route (`GET /chunks/{id}/pm-items`, fronted by `blizzard runner pm-items`): the runner forwards to the hub, the hub calls the vendor.** | "Workers never talk to the hub" and "the build node fetches through the hub's API" could not both hold, and no CLI verb existed for the read at all. The chain keeps hub location and credentials out of workers entirely. The runner web app's no-proxy rule is untouched — it governs the browser's fleet pages, not the worker data path. | [design/architecture.md](../design/architecture.md) |
| D-085 | 2026-07-13 | **`epic:gates` moves into `milestone:mvp`: gate nodes, the runner-config gate (D-032/D-073), Decision parking (D-045), and gate surfacing through the CLI and the board (D-052).** | D-052 already promised decision surfacing "from day one," and the parking machinery is shared with ask/answer — the cut saved little while making the HITL→HOTL dial, the product's trust story, unavailable to the first user. Gate decisions via CLI and board are MVP-necessary. | [product/mvp.md](../product/mvp.md) |
| D-086 | 2026-07-13 | **MVP conflict routing is intra-graph: the deliver node's conflict outcome routes only within the chunk's own graph — an authored `conflict` choice entry if present, else the graph's entry node. The conventional cross-graph `merge` workflow (D-058) departs the MVP with `epic:migration`.** | Chunks landing overnight will conflict routinely, and the default conflict path must not depend on migration machinery the MVP does not ship; within-graph routing is an ordinary transition. D-058's convention stands once migration lands. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-087 | 2026-07-13 | **MVP workers run permission-skipped (auto-accept), configured per runner. Two epics are minted: `epic:security` — worker lockdown: permission profiles, network/credential blast radius, force-push protection, per-node permission config — and `epic:cost` — budget caps, spend telemetry as facts, cost-based model routing, a spend kill-switch. Both post-MVP.** | Unattended operation is impossible without a permission stance, so the MVP states the honest one and gives the real design a home; token-spend runaway is a first-order hazard that deserves an epic owner rather than an open question with none. | [product/epics.md](../product/epics.md) |
| D-088 | 2026-07-13 | **The hub can forcibly detach a chunk from its runner: `POST /chunks/{id}/detach` (CLI and board) appends `route.released` — the chunk re-derives `ready`, and the next claim's lease floor (D-035/D-044) fences the old runner's late writes. The detached runner learns over its pull: kill the worker, release the environments.** | Nothing ever un-routed a chunk — a permanently dead runner left its chunks `running` forever, with terminal `stop` the only abandonment path. Detach is the superadmin's "this runner is gone, release its work," reusing the reassignment machinery the design already described. | [design/hub/index.md](../design/hub/index.md) |
| D-089 | 2026-07-13 | **The node envelope carries all of the chunk's artifacts — every artifact name at its latest-by-epoch entry — never a per-node selection.** | "Relevant artifacts" had no selection rule; all-latest is deterministic and needs no configuration, and payloads are modest (the hub holds pointers and assets, never code — D-012). A selection mechanism can come later if envelopes bloat. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-090 | 2026-07-13 | **Advancement survives response loss: `GET /chunks/{id}/envelope` returns the chunk's current node envelope idempotently, and a transition flush acked as already-applied (seq ≤ high-water mark) directs the runner to that read.** | The apply-response carried the next envelope, but a lost HTTP response after a committed transition left the runner re-sending a seq the hub merely acks — wedged at a boundary it had already crossed. An idempotent current-envelope read closes the gap without persisting per-seq response replicas. | [design/hub/api.md](../design/hub/api.md) |
| D-091 | 2026-07-13 | **Multi-repo delivery is serialized best-effort reconciliation: the deliver step lands a chunk's git-commit artifacts one repo at a time, recording a per-repo landed fact (`delivery.repo_landed`); on any failure the step re-runs as reconciliation — already-landed repos are detected and skipped, only the remainder is attempted. `delivery.landed` (terminal) fires only when every repo has landed; a conflicting remainder routes per D-086 with partial lands retained and reconciled on redelivery.** | Forges cannot make a cross-repo landing atomic, and the model pretended otherwise — partial failure had no semantics at all. Reconciliation makes the partial state explicit, idempotent, and retryable; the guarantee is convergence, and cross-repo atomicity is named as impossible rather than implied. | [design/workflow-engine.md](../design/workflow-engine.md) |
| D-092 | 2026-07-13 | **The adapter contract accepts harness-assigned session identity and grows a conformance suite: `spawn` takes a session *hint* and returns the actual `{session_id, pid}`, recorded as facts at spawn-return — harnesses that honor pre-assignment (Claude Code) return the hint, those that self-assign (OpenCode, Codex) return theirs; correlation keys on the runner-minted lease id throughout. Every adapter ships executable per-harness conformance tests — `blizzard runner selftest` grows into the suite — and the per-harness comparison table is version-pinned observation, never contract.** | The four-function interface assumed pre-assigned ids and pid-only returns that two of the three harnesses cannot deliver. The lease id is spawn-injected, so session identity can safely become a spawn-return fact; and CLI surfaces drift monthly, so executable conformance tests — not documentation — are what keep adapters honest (hardening D-008 and the hard-problems stance). | [design/harness-adapters.md](../design/harness-adapters.md) |
| D-093 | 2026-07-13 | **Ingestion enforces active-pointer uniqueness: `POST /chunks` rejects (409, returning the existing chunk id) a pointer already held by a live — non-terminal — chunk; re-ingest is legal once every prior holder is `done`, `stopped`, or discarded.** | D-047's re-ingest-mints-fresh silently allowed the same PM item to be worked twice in parallel, violating the mission's zero-duplicated-work guarantee at the front door. Uniqueness among live chunks preserves the ephemerality without the race. | [design/hub/index.md](../design/hub/index.md) |
||||||| Stash base
| D-094 | 2026-07-13 | **Backend language and stack: Python across hub, runner, and CLI — FastAPI (HTTP APIs + SSE), SQLAlchemy (persistence), click (CLI), uv (packaging); further library needs default to what winter-cli and the other winter projects already use rather than new picks.** | One language across all three components keeps the one-binary shape (D-061) trivial, and reusing winter's proven stack carries its harness conventions, exemplars, and agent familiarity over instead of rebuilding them. | [implementation/tech-stack.md](../implementation/tech-stack.md) |
| D-095 | 2026-07-13 | **Both stores are SQL-configurable: the hub store and the runner store each run on sqlite or postgres, selected by configuration, with sqlite the default and neither daemon depending on backend-specific behavior; tests run against sqlite, with postgres compatibility held by staying inside SQLAlchemy's portable surface rather than a duplicated test matrix. Refines D-003, which stands: sqlite (WAL) remains the broker default — the rejection there was of non-SQL alternatives (flock/Redis/git-native/GitHub), and postgres preserves the ACID CAS semantics that decision rests on.** | Backend-agnosticism is cheap to hold from day one under SQLAlchemy and expensive to retrofit once backend-specific behavior leaks in. | [implementation/tech-stack.md](../implementation/tech-stack.md) |
| D-096 | 2026-07-13 | **The web app is Angular as a plain client-rendered SPA — no SSR, no prerendering — with FastAPI serving the built assets (StaticFiles with an SPA fallback) from the same process and origin as `/api` and the SSE streams.** | SSR earns its keep on SEO and anonymous cold-visit first paint, and an authless operator board has neither; every interesting view is SSE-fed and renders client-side after data arrives regardless, and prerendering would demand build-time-enumerable routes to prerender fully dynamic pages. Same-process serving is the only topology that keeps D-061's one-binary promise with no caveats, and Angular's per-route render modes keep the choice reversible per page. | [implementation/tech-stack.md](../implementation/tech-stack.md) |
| D-097 | 2026-07-13 | **Frontend library set: Angular CDK plus custom components over the mission-control mockup's design tokens — no Material or other styled component library; TanStack Query for Angular for request/response server reads; signals component-locally and NgRx SignalStore for shared feature state (no classic NgRx Store); a hand-rolled SSE service (EventSource → RxJS → signals) whose events invalidate or patch TanStack queries; zoneless change detection; Vitest for frontend unit/component tests. No chart library, no Tailwind.** | The board's bespoke mission-control aesthetic makes a styled component library a fight rather than a leg up, while the CDK gives behavior without a look and the mockup already is the design system (a portable custom-property token block). TanStack Query owns request-cache semantics so the SSE layer only has to patch and invalidate, and every pick is boring, deeply documented, and signals-native — the right defaults for agent-built code. | [implementation/tech-stack.md](../implementation/tech-stack.md) |
| D-098 | 2026-07-13 | **Trunk-based development with milestones as tags: `master` is the only long-lived branch (protected, always releasable, PR-only); a milestone is a `v0.<milestone>.<patch>` tag whose push triggers the GitHub Actions release build; `-rc.N` tags cut prerelease candidates; backport branches (`release/v0.x`) are created lazily from a tag only when a shipped milestone needs a fix after master moved on, and are deleted when support ends. CI is GitHub Actions.** | Long-lived milestone branches are where merge hell lives, and an agent fleet producing many small PRs amplifies it; with trunk-based flow nothing ever merges back because work never leaves master. Tags make "milestone build" an event CI reacts to rather than a parallel line to maintain. | [implementation/build.md](../implementation/build.md) |
| D-099 | 2026-07-13 | **Store migrations are Alembic, manual, and CLI-driven — never automatic: two independent migration trees (hub store, runner store), every revision with explicit up/down, revision-level idempotent application, scripts confined to SQLAlchemy's portable surface (D-095). `blizzard hub|runner init <dir>` scaffolds the runtime environment idempotently (config, data dir, DB migrated to head); `migrate` / `migrate --down` applies and reverses; daemons refuse to start on a schema-revision mismatch, naming the migrate command.** | Unattended daemons must never rewrite their own data model as a startup side effect — version skew should fail loud with the fix in the error message. Alembic's version table gives idempotent re-runs for free, and proper down scripts keep every upgrade rehearsable and reversible. | [implementation/build.md](../implementation/build.md) |
| D-100 | 2026-07-13 | **The Angular apps consume a TypeScript client generated by openapi-ts from the FastAPI-exported OpenAPI specs, and the generated client is committed — with a CI drift gate that regenerates and fails on any uncommitted difference.** | Generated-and-committed makes API changes visible in review diffs, lets agents read the client as source, and needs no codegen at install; the drift gate makes a stale client unmergeable, removing the one risk of committing generated code. | [implementation/build.md](../implementation/build.md) |
| D-101 | 2026-07-13 | **Quality toolchain: ruff (lint + format) and pyright for Python; eslint for TypeScript/Angular; no prettier. Gates on from the first commit.** | One tool per concern, aligned with the winter-cli standards agents already know; a second formatter is a second opinion to reconcile, not a capability. | [implementation/tech-stack.md](../implementation/tech-stack.md) |
| D-102 | 2026-07-13 | **Logging via structlog with renderer selected by configuration: JSON output when agents, services, or CI consume logs; a human console renderer for interactive runs — TTY-detected default, config/env override. One call-site convention regardless of renderer.** | The same daemons are read by agents (who want parseable structure) and humans (who want legible color) — a structured-first framework makes output format a rendering decision instead of two logging disciplines. | [implementation/tech-stack.md](../implementation/tech-stack.md) |
| D-103 | 2026-07-15 | **Ingest mints a chunk in a `not_ready` resting state by default; a runner never claims it until it is explicitly promoted. `not_ready` is a new derived status (a `chunk.promoted` fact absent), sitting just above `ready` in the precedence and below every post-claim state; `POST /chunks/{id}/promote` (and `blizzard hub promote`) appends the fact that flips it to `ready`. The board shows not-ready chunks in their own backlog column. A migration back-fills a `chunk.promoted` fact for every pre-existing chunk so upgrades leave in-flight work claimable.** | Ingest and "start working" were the same action, with no way to land an issue on the board while it is still being refined or awaiting operator inspection. A resting state keeps ingest cheap and reversible while making "ready for an agent" an explicit, auditable act — and, being derived from a fact (D-004), it needs no stored status column and no runner-side change (the ready-only queue peek already excludes it). | [design/domain/work.md](../design/domain/work.md) |
| D-104 | 2026-07-16 | **Work reaches `master` three ways, and PR-only is not the rule: an agent working by hand in a local feature environment pushes directly to `origin/master` (rebased onto it first, linear history, one landed unit of work per commit); a fleet chunk lands through the hub's `deliver` node in one of its two authored modes — `merge-to-main` (the hub lands it, no human step) or `open-pr` (the hub parks the chunk on an open PR for a human to resolve, then completes it from the outcome). `master` is not branch-protected. Supersedes D-098's "protected, all work arrives as PRs" clause only; the rest of D-098 — trunk-based, milestones as tags, lazy backports — stands.** | D-098's PR-only clause never matched the model around it: D-014 had already superseded PR-as-default with a baseline where agents merge to main and every human gate is opt-in configuration, and the `merge-to-main` deliver mode is exactly that baseline implemented. Nor did it match practice — `master` is unprotected and carries direct commits. A PR that no path waits on is ceremony rather than a gate; the real gate is the local check run plus the tiers CI already runs on every push to `master`. | [implementation/build.md](../implementation/build.md) |
| D-105 | 2026-07-16 | **Runner pause has two independent brakes and effective paused is their OR; each is cleared only on the surface that set it.** The runner's own brake is local state on the runner singleton (`PATCH /runner`, facts append and the flag derives), set machine-locally, adhered to by FILL, and working with the hub unreachable — the operator contract's standing requirement; it is reported up as `runner.locally_paused` / `runner.locally_resumed` through the outbound buffer (D-069) so the board can render *which* brake is on, and the hub only ever reads it. The hub's brake stays the registry flag (D-043): the fleet coercing a runner, cleared with `blizzard hub resume`. `blizzard runner start` clears only the local one. Resolves the local-vs-hub pause reconciliation open question. | They are different acts by different actors — a runner declining work is not the fleet stopping it — so collapsing them into one flag would make the board unable to say who stopped a runner, and would let either surface silently clear the other's decision. OR needs no precedence rule: a brake is a brake. Reporting the local flag upward is what keeps D-043's legibility promise, since the hub can only render what it holds. | [design/runner/api.md](../design/runner/api.md) |
